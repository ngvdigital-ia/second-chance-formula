# Second Chance Formula

Local VSL clone created from a single HTTP capture of the supplied reference.

Current state:

- `waiting-video`: official VTurb embed was not supplied.
- `waiting-tracking`: official Meta Pixel and UTMify IDs were not supplied.
- Checkout is intentionally not exposed; the future CTA remains controlled by VTurb.

Entry point: `vsl/index.html`.
